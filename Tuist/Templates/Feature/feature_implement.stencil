//
//  {{ name }}.swift
//
//  {{name}}
//
//  Created by {{author}}
//

import SwiftUI
import {{ name }}FeatureInterFace
import ComposableArchitecture

public struct {{ name }}Feature: {{ name }}Interface {
  public let store: StoreOf<{{ name }}Reducer>

  public init(store: StoreOf<{{ name }}Reducer) {
    self.store = store
  }
  
  public func startView() {
    return AnyView({{ name }}View(store: store))
  }
}
