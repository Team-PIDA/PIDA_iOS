//
//  {{ name }}DataTests.swift
//
//  {{name}}
//
//  Created by {{author}}
//

import XCTest
import Dependencies
@testable import {{ name }}DataInterface
@testable import {{ name }}DomainInterface
@testable import Core

final class {{ name }}RepositoryTests: XCTestCase {

    func testFetchData() async throws {
        let networker = MockNetworker()
        let repository = {{ name }}RepositoryImpl(networker: networker)

        let result = try await repository.requestMockData()
        XCTAssertEqual(result, "Mocked API Data")
    }
}

final class {{ name }}RepositoryImpl: {{ name }}Repository {
    func requestMockData() async throws -> String {
        return "Mocked API Data"
    }
}
